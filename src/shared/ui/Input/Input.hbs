<div class="input-wrapper{{#if isConfirmPassword}} input-wrapper--confirm-password{{/if}}">
  <input 
    type="{{type}}"
    class="input-wrapper__input"
    placeholder="{{placeholder}}"
    value="{{value}}"
    name="{{name}}"
  />
  <span class="input-wrapper__error">{{error}}</span>
</div>