<div class="task-card {{#if isCompleted}}task-card--completed{{/if}} {{#if isEditing}}task-card--editing{{/if}}"
     data-task-id="{{id}}">
    <div class="task-card__content">
        {{#if isEditing}}
            <textarea class="task-card__input" placeholder="Введите задачу...">{{title}}</textarea>
            <div class="task-card__actions">
                <span class="task-card__save">
                    ✓
                </span>
            </div>
        {{else}}
            <span class="task-card__checkbox"></span>
            <span class="task-card__title">{{title}}</span>
            <div class="task-card__actions">
                <span class="task-card__edit">
                    <img src="/images/edit-icon.svg" alt="Редактировать" />
                </span>
                <span class="task-card__delete">
                    ✕
                </span>
            </div>
        {{/if}}
    </div>
</div>